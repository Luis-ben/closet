App({
  globalData: {
    apiBaseUrl: "http://localhost:3000/api",
    token: "mock-token-user_mock_001"
  },

  onLaunch() {
    // Mock token is used while the backend still runs with an in-memory store.
  }
});

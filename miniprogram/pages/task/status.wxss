.status-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding-bottom: calc(48rpx + env(safe-area-inset-bottom));
}

.status-card {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 58rpx 34rpx;
  border: 1rpx solid #E8EAED;
  border-radius: 16rpx;
  background: #FFFFFF;
  box-shadow: 0 8rpx 24rpx rgba(60, 64, 67, 0.1);
  text-align: center;
}

.loader-wrap {
  position: relative;
  width: 186rpx;
  height: 186rpx;
  margin-bottom: 38rpx;
}

.loader-ring {
  width: 186rpx;
  height: 186rpx;
  box-sizing: border-box;
  border: 8rpx solid #E8F0FE;
  border-top-color: #111111;
  border-radius: 50%;
  animation: spin 1.1s linear infinite;
}

.loader-wrap.success .loader-ring,
.loader-wrap.failed .loader-ring {
  animation: none;
}

.loader-wrap.failed .loader-ring {
  border-color: #D64545;
}

.loader-core {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 74rpx;
  height: 74rpx;
  transform: translate(-50%, -50%);
  border-radius: 16rpx;
  background: #E8F0FE;
  box-shadow: inset 0 0 0 2rpx rgba(26, 115, 232, 0.18);
}

.loader-glow {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 116rpx;
  height: 116rpx;
  transform: translate(-50%, -50%);
  border-radius: 50%;
  background: rgba(109, 139, 255, 0.12);
}

.status-text {
  color: #202124;
  font-size: 40rpx;
  font-weight: 800;
}

.status-hint {
  margin-top: 12rpx;
  color: #5F6368;
  font-size: 26rpx;
}

.progress-track {
  width: 100%;
  height: 14rpx;
  margin-top: 46rpx;
  overflow: hidden;
  border-radius: 999rpx;
  background: #E8F0FE;
}

.progress-bar {
  height: 100%;
  border-radius: 999rpx;
  background: linear-gradient(90deg, #1A73E8, #111111);
  transition: width 0.3s ease;
}

.progress-label {
  margin-top: 14rpx;
  color: #5F6368;
  font-size: 24rpx;
  font-weight: 700;
}

.wait-copy {
  display: grid;
  gap: 6rpx;
  margin-top: 34rpx;
  color: #5F6368;
  font-size: 24rpx;
}

.error-card {
  width: 100%;
  box-sizing: border-box;
  margin-top: 30rpx;
  padding: 22rpx;
  border: 1rpx solid rgba(214, 69, 69, 0.28);
  border-radius: 16rpx;
  background: #FFF5F5;
  color: #D64545;
  font-size: 26rpx;
  line-height: 1.45;
}

.status-actions {
  display: grid;
  gap: 18rpx;
  margin-top: 28rpx;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}
